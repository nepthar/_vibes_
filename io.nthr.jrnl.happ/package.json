{
  "name": "jrnl-web",
  "version": "1.0.0",
  "private": true,
  "description": "Jrnl — the journal app as a static SPA + API. No dependencies; uses node:sqlite.",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test",
    "seed": "node scripts/seed.js",
    "signup-token": "node scripts/signup-token.js"
  }
}
